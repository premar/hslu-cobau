package ch.hslu.cobau.minij.symbol;

public enum SymbolKind {
    PROCEDURE,           // symbol for procedure
    RECORD,              // symbol for record
    FIELD,               // symbol for record field
    VARIABLE,            // symbol for variable
    PARAMETER,           // symbol for a non-reference parameter
}
